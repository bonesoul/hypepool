namespace Hypepool.Core.Internals.Registries
{
    public interface IRegistry
    {
        /// <summary>
        /// Runs the registery so it can register it's contents.
        /// </summary>
        void Run();
    }
}
